import {
  titleTemplate,
  stopImmediatePropagationTemplate,
  stopPropagationTemplate,
} from ".";

export const layout = `
${titleTemplate}
${stopImmediatePropagationTemplate}
${stopPropagationTemplate}`;
